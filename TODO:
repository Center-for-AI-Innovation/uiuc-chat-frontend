TODO:
[ ] understand HomeContext
  [ ] What does updateConversationMutation do?

[ ] React Query is used with fetch
- which one should be prioritized?
- use react-query over fetch
  [ ] Confirm QueryClientProvider placement and app-wide access
  [ ] Build typed fetch helper (throw on !ok, parse JSON)
  [ ] Swap component GET calls to useQuery with stable keys
  [ ] Replace mutations with useMutation + invalidation
  [ ] Keep streaming/endpoints on custom fetch hooks
  [x] Remove legacy useFetch wrapper once migration completes

[-] (做不到因为用了react OIDC context) 使用 getServerSideProps 来动态渲染 chat 页面, 检查用户认证状态
[ ] 引入 Redux,代替 Home Context 中的 State Variables
[ ] 使用 Tailwind CSS instead of Mantine UI

react-query, fetch, NextJS

- [ ] 理解LLM Service Provider的作用,理解它们和什么component进行交互,调用了什么API,在什么时候做了什么事。
- [ ] 重写web llm module
